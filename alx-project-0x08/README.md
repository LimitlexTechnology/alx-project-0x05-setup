# alx-project-0x08

Image Generation App built with Next.js and React (state tracking version)

## Getting Started

Install dependencies:

```bash
npm install
```

Run the dev server:

```bash
npm run dev -- -p 3000
```

Open http://localhost:3000
